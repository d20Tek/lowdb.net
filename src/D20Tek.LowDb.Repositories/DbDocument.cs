namespace D20Tek.LowDb.Repositories;

/// <summary>
/// Serves as the base type for LowDb database documents used with the repository pattern.
/// A document typically exposes one or more <see cref="HashSet{T}"/> collections that a
/// repository projects to and manages as entity sets.
/// </summary>
public abstract class DbDocument
{
}
